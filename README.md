# 🏆 پلتفرم طلا آنلاین - Gold Trading Platform

ایک پلتفرم مدرن و ایمن برای خرید و فروش طلا به صورت گرمی (Sut) با دیزاین بی‌نقص و تجربه کاربری عالی.

## ✨ ویژگی‌های اصلی

### 🔐 احراز هویت و امنیت
- ثبت‌نام با OTP (کد یکبار مصرف)
- تایید شماره تلفن
- احراز هویت KYC کامل (اطلاعات فردی، مستندات، حساب بانکی)
- هر کد ملی تنها یک‌بار می‌تواند ثبت‌نام کند

### 💰 کیف پول
- نمایش موجودی طلا (سوت)
- نمایش موجودی تومان
- تاریخچه تراکنش‌ها
- آنی‌ روزرسانی موجودی

### 📊 معاملات
- **فروش طلا**: تبدیل سوت به تومان (1 سوت = 1000 تومان)
- نرخ ارز ثابت
- دکمه‌های سریع (25%، 50%، 75%، 100%)
- **خرید طلا**: به‌زودی

### 🎰 گردونه شانس
- هر روز یک بار چرخاندن رایگان
- 8 خانه با جوایز (50 تا 50,000 سوت یا خالی)
- انیمیشن‌های جذاب
- نمایش جایزه برنده‌شده

### 👥 سیستم معرفی (Referral)
- کد معرفی منحصر‌بفرد برای هر کاربر
- 1,000 سوت پاداش برای هر معرفی موفق
- برداشت فقط بعد از 5 معرفی تایید‌شده
- لیست تفصیلی معرفی‌ها

### 👤 پروفایل
- نمایش اطلاعات کاربری
- تکمیل احراز هویت
- آپلود مستندات
- وضعیت احراز هویت

### 📱 طراحی
- **Mobile-First**: بهینه‌شده برای موبایل
- **RTL**: پشتیبانی کامل فارسی/عربی
- **Modern UI**: طراحی حداثی 2025
- **Responsive**: کار کردن در تمام سایزهای صفحه
- **Smooth Animations**: انیمیشن‌های جذاب

## 🛠 فناوری‌های استفاده‌شده

### Frontend
- **React 18** - UI Framework
- **TypeScript** - Type Safety
- **Vite** - Build Tool
- **Tailwind CSS** - Styling
- **Framer Motion** - Animations
- **React Query** - Data Fetching
- **React Hook Form** - Form Management
- **Zod** - Schema Validation
- **Radix UI** - UI Components
- **Wouter** - Routing

### Backend
- **Express.js** - Web Server
- **TypeScript** - Type Safety
- **Drizzle ORM** - Database
- **PostgreSQL** (Neon) - Database
- **Multer** - File Upload
- **Telegram Bot API** - Notifications
- **Custom SMS API** - OTP Delivery

## 📦 نصب و اجرا

### پیش‌نیازها
- Node.js 18+
- npm یا yarn

### نصب

```bash
# نصب وابستگی‌ها
npm install

# تنظیم متغیرهای محیط
cp .env.local.example .env.local
# ویرایش .env.local و تنظیم توکن‌ها
```

### توسعه

```bash
# اجرای در حالت توسعه
npm run dev

# بررسی نوع‌ها
npm run check

# بناء برای تولید
npm run build

# اجرای در حالت تولید
npm start
```

## 🔧 متغیرهای محیط

```env
DATABASE_URL=postgresql://user:password@localhost:5432/db
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
SMS_API_TOKEN=your_sms_token
PORT=5000
VITE_API_URL=http://localhost:5000
```

## 📋 ساختار پروژه

```
PastedOtp/
├── client/                 # Frontend React
│   ├── src/
│   │   ├── pages/         # صفحات اصلی
│   │   ├── components/    # کامپوننت‌های React
│   │   ├── lib/           # توابع کمکی
│   │   ├── hooks/         # Custom Hooks
│   │   └── App.tsx        # اپلیکیشن اصلی
│   └── public/            # فایل‌های استاتیک
├── server/                # Backend Express
│   ├── routes.ts         # API Routes
│   ├── storage.ts        # Database
│   ├── db.ts             # Database Config
│   └── app.ts            # Express Setup
├── shared/               # Shared Schema
│   └── schema.ts         # Database Schema
└── package.json          # Dependencies
```

## 🔗 API Routes

### احراز هویت
- `POST /api/auth/send-otp` - ارسال کد OTP
- `POST /api/auth/verify-otp` - تایید کد
- `POST /api/auth/register` - ثبت‌نام کاربر

### کاربر
- `GET /api/user/:userId` - دریافت اطلاعات کاربر

### KYC
- `POST /api/kyc/submit` - ارسال مستندات احراز هویت

### گردونه شانس
- `GET /api/wheel/can-spin/:userId` - بررسی امکان چرخش
- `POST /api/wheel/spin` - چرخاندن گردونه

### معاملات
- `POST /api/trading/sell` - فروش طلا
- `GET /api/transactions/:userId` - دریافت تاریخچه

### معرفی
- `GET /api/referrals/:userId` - دریافت لیست معرفی‌ها

## 🎨 رنگ‌های قالب

- **Primary**: Gold/Amber (#D4AF37 ~ RGB(212, 175, 55))
- **Secondary**: Orange (#FB923C ~ RGB(251, 146, 60))
- **Accent**: Green (Success #22C55E)
- **Destructive**: Red (Error #FF4444)

## 📱 صفحات

1. **Home** (`/`) - داشبورد اصلی
2. **Register** (`/register`) - ثبت‌نام
3. **Wallet** (`/wallet`) - کیف پول
4. **Lucky Wheel** (`/lucky-wheel`) - گردونه شانس
5. **Trading** (`/trading`) - خرید و فروش
6. **Profile** (`/profile`) - پروفایل و احراز هویت
7. **Referral** (`/referral`) - دعوت دوستان

## 🚀 نکات مهم

- برای استفاده از فروش، کاربر باید احراز هویت کامل را انجام دهد
- برای برداشت تومان، کاربر باید حداقل 5 معرفی موفق داشته باشد
- هر روز تنها یک بار می‌توان گردونه را چرخاند
- هر کد ملی تنها یک‌بار ثبت‌نام می‌تواند داشته باشد

## 📞 پشتیبانی

برای پشتیبانی یا ایجاد مشکل، لطفاً تماس بگیرید.

---

**توسعه‌دهنده**: Reza Hashmyan
**ورژن**: 1.0.0
**تاریخ**: November 2025
